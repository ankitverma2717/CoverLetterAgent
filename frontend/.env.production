# Production API URL for Render deployment
# IMPORTANT: This URL should NOT include /api/ at the end
# The code already adds /api/v1/... to the endpoints
NEXT_PUBLIC_API_URL=https://coverletteragent.onrender.com
